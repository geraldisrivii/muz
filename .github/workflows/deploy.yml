name: 🚀 Deploy to Production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 🔑 Setup SSH agent
        uses: webfactory/ssh-agent@v0.9.1
        with:
          ssh-private-key: ${{ secrets.SSH }}

      - name: Create .env file for frontend
        env:
          API_URL: ${{ vars.API_URL }}
        run: |
          ssh -o StrictHostKeyChecking=no ${{ vars.SERVER_USER }}@${{ vars.SERVER_HOST }} << 'EOF'
             echo "✈️ make frontend .env"

            cd /var/www
            cp /var/www/apps/frontend/template.env /var/www/apps/frontend/.env
            envsubst < /var/www/apps/frontend/template.env > /var/www/apps/frontend/.env
          EOF

      - name: 🚀 Deploy via SSH
        run: |
          ssh -o StrictHostKeyChecking=no ${{ vars.SERVER_USER }}@${{ vars.SERVER_HOST }} << 'EOF'
            echo "✈️ Pulling latest code..."
            cd /var/www

            docker compose down
            
            git reset --hard
            git pull

            docker compose up --build -d

            echo "✅ Deployment finished."
          EOF
